import { ArrFilterPipe } from './arr-filter.pipe';

describe('ArrFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
